#pragma once

namespace wikigraphs {

class Driver {
 public: 
  /**
   * Runs the main loop to allow the user to control the program.
   * 
   * @param argc number of inputs from command line
   * @param argv command line arguments
   */
  void Run(int argc, char **argv);
 private:
};

}